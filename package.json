{
    "name": "mfe-webpack-typescript",
    "private": true,
    "scripts": {
        "build": "yarn workspaces run build",
        "serve": "wsrun --parallel serve",
        "start": "concurrently \"wsrun --parallel start\" \"wsrun --parallel start\""
    },
    "workspaces": [
        "packages/*"
    ],
    "devDependencies": {
        "concurrently": "^8.2.0",
        "wsrun": "^5.2.4"
    }
}